using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileFlag : Tiled
{
   
    // Start is called before the first frame update
    void Start()
    {
     
        values = 4; //TILE: Tile 1벽 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
