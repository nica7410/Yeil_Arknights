using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileEnd : Tiled
{
    // Start is called before the first frame update
    void Start()
    {
        values = 3; //TILE: Tile 3엔드
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
