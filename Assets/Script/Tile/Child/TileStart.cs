using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileStart : Tiled
{
    // Start is called before the first frame update
    void Start()
    {
     
        values = 2;//TILE: Tile 2스타트
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
